#include "main.h"
/**
 * _puts - function that prints a string, followed by a new line
 * @str: string to be printed
 *
 */

void _puts(char *str)
{
	for (int = 0; i < _strlen(char *str); i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}

